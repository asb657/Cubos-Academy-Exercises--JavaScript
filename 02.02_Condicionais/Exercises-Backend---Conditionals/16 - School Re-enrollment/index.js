const studentAge = 11;
const hasGuarian = true;

const reenrollment = (hasGuarian || studentAge > 18 ? 
    console.log(`Re-enrollment completed successfully`) : 
    console.log(`Cannot re-enroll`));

